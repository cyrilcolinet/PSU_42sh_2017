/*
** EPITECH PROJECT, 2018
** my_str_to_array_42
** File description:
** 42
*/

#include "42.h"

char **my_str_to_array_42(char *str, char delim)
{
	char **tab = NULL;
	int count = 0;

	if (str == NULL)
		return (NULL);

	return (tab);
}
